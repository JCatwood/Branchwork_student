# Student answers

The app writes one formatted JSON file here for each assessment. These files
contain the student ID, save time, and answers. Multiline R code is stored as a
normal JSON string with all line breaks preserved.

Students should commit and push these JSON files to their private GitHub
repository so the instructor can review them.
